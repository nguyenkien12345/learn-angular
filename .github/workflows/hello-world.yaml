name: hello world by nguyen trung kien
on:
  push:
    paths:
      - '!note.txt'
      - '!README.md'
      - '!.vscode/**'
      - '!.editorconfig'
      - 'src/**'
      - '.github/**'
    branches:
      - 'master'
      - 'release/**'
      - 'staging/**'
      - '!develop/**'
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODAY='02/08/2023'" >> $GITHUB_ENV
      - run: echo Hello world, today is $TODAY
